// ==============================================================================================
// scope/index
// ==============================================================================================
// variable ========================================
var bodyModifier = $('body').attr('data-modifier');
// function ========================================
if(bodyModifier.match(/^index$/)) {
	require('../component/loading');
};