// ==============================================================================================
// scope/index
// ==============================================================================================
// require ========================================
$ = require('jquery');
// variable ========================================
var bodyModifier = $('body').attr('data-modifier');
// function ========================================
if(bodyModifier.match(/^home$/)) {
	require('../component/loading');
};