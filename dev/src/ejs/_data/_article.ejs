<%#
=====================================================================================================
_data/_article
=====================================================================================================
%>
<%
var articleTar = ['article', 'news'];
var articleArray = [];
%>
<%# ---------------------------------------------------------------------------------------------- %>
<%
Object.keys(articleTar).forEach(function(index) {
	var articleCat = articleTar[index];
	articleArray.push(articleCat);
	articleArray[articleCat] = [];
	Object.keys(page).forEach(function(value, index) {
		if (!value.indexOf(articleCat + '/') && !~value.indexOf('index')) {
			articleArray[articleCat].push(page[value]);
		}
	});
});
%>
<%# ---------------------------------------------------------------------------------------------- %>
<% article = articleArray; %>