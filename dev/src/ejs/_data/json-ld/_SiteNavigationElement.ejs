<%#
=====================================================================================================
_data/json-ld/_SiteNavigationElement
=====================================================================================================
%>
{
	"@context": "http://schema.org/",
	"@type": "SiteNavigationElement",
	"name": [
		<% data.siteNav.forEach(function(value, key) { %>
			<% if (data.siteNav.length - 1 != key) { %>
				"<%- page[value].title %>",
			<% } else { %>
				"<%- page[value].title %>"
			<% } %>
		<% }) %>
	],
	"url": [
		<% data.siteNav.forEach(function(value, key) { %>
			<% if (data.siteNav.length - 1 != key) { %>
				"<%- data.site.url %><%- page[value].absUrl %>",
			<% } else { %>
				"<%- data.site.url %><%- page[value].absUrl %>"
			<% } %>
		<% }) %>
	]
}
